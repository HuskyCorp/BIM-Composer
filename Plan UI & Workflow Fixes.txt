Plan: UI & Workflow Fixes
Execution Order
Phase	Issues	Notes
Phase 1 (parallel)	1, 3, 4	CSS/HTML only, no state dependencies
Phase 2	6	Must clean statement.usda before fixing history
Phase 3	5	Can now use cleaner stagedPrims paths
Phase 4	2	Pset staging hooks into clean pipeline
Phase 5	7	Final terminology pass over all touched files
Issue 1 — Custom Psets Light Mode Shows Dark Colors
Root cause: psetPropertyModal.js hardcodes ~35 dark hex values inline in the innerHTML template (#1e1e1e, #e0e0e0, etc.).

Fix:

Create css/pset-modal.css with semantic classes (.pset-modal-content, .pset-item, .pset-item.selected, etc.) using light colors by default
Add dark overrides in css/dark-theme.css under body.dark-theme
Replace all inline style="" in psetPropertyModal.js with class="" references
Remove 6 JS mouseenter/mouseleave handlers — replace with CSS :hover / :focus
Replace JS class-toggling with classList.add/remove('selected')
Add <link> to pset-modal.css in index.html
Issue 2 — Custom Psets Don't Trigger Staged Changes
Root cause: src/components/properties/AttributeUpdater.js applyPsetDictionary() never calls actions.addStagedChange(). Compare to applyAttributeChange() which does.

Fix:

In applyPsetDictionary(), after validation, dispatch a staged change { type: "psetEdit", targetPath, propertyName: psetName, ... }
In stagingPanelController.js, add a "psetEdit" display branch in the preview renderer and classifyCommitType()
Issue 3 — Drag & Drop Prims in Hierarchy
Root cause: No drag-and-drop implemented in src/components/sidebar/hierarchyPanelController.js.

Fix:

Use HTML5 drag-and-drop with event delegation on #usdaOutliner
dragstart → store sourcePath in dataTransfer
dragover / dragleave → toggle .drag-over CSS highlight class
drop → call reparentPrim(sourcePath, newParentPath):
Guard: block if in history mode, or if drop would create a circular hierarchy
Write a reparent entry to the source layer file
Dispatch { type: "reparent", oldPath, targetPath } staged change
Recompose stage + update view
Add .dragging and .drag-over styles to css/outliner.css
Issue 4 — Commit UI Redesign
Root cause: Modal has rows="3", poor spacing, uses "Commit" git terminology. Inline styles leak through JS in preview list.

Fix:

Redesign commit modal in index.html: rows="8", sectioned layout (author row → pending changes list → revision note textarea)
Add professional classes to css/modal.css: .commit-preview-list, .commit-message-textarea, .commit-author-row, .commit-section-label
Add dark overrides in css/dark-theme.css
In stagingPanelController.js, remove inline li.style.cssText; add type icons (📦 ✏️ 📋) to preview items
Issue 5 — History Freezing / Selection Bleed
Root cause: When entering history mode, primSelected events from live scene leak into the properties panel. reconstructStateAt() is synchronous and heavy.

Fix:

In timelineController.js: dispatch primSelected with null payload before setupTimeline() to clear selection
Dispatch another clear after reconstructStateAt() completes
In src/components/properties/index.js: add early return if (store.getState().isHistoryMode) return; at the top of handlePrimSelected
Debounce setupTimeline() calls (50ms) to prevent freezing from rapid clicks
Issue 6 — statement.usda Contains Full USDA Hierarchy
Root cause: primStaging.js calls composePrimsFromHierarchy() and passes the full USDA text as serializedPrims into the log entry. This bloats statement.usda and couples history replay to serialized prim data.

Fix (requires changes across 4 files):

primStaging.js: Remove serializedPrims from logToStatement(); remove composePrimsFromHierarchy() call
src/viewer/usda/usdaComposer.js: In composeLogPrim(), remove the block that serializes serializedPrims into the log prim body. Only write scalar metadata + custom string[] stagedPrims
src/viewer/usda/parser/logParser.js: Remove parsePrimTree(logBody) call; set commit.serializedPrims = []
timelineController.js: In reconstructStateAt(), remove the serializedPrims replay block. Replace with path-based lookup using commit.stagedPrims — mark prims at those paths as staged at that entry. Update buildCommitDiff() to diff path arrays instead of parsed prim objects
Issue 7 — AEC Terminology (Final Pass)
Agreed mapping (Option A — Construction Analogy):

Old (Git)	New (AEC)
Commit	Record
Staged Changes	Pending Changes
Stage (verb)	Mark
History	Record Log
History Mode	Record Log Mode
Commit message	Revision Note
Confirm/Commit button	Record Changes
Files to update: index.html, stagingPanelController.js, timelineController.js, any tooltip title="" attributes throughout

Ready to start implementing? I'd suggest we tackle Phase 1 (Issues 1, 3, 4) in parallel first, then proceed to the statement.usda cleanup in Issue 6. Say the word and I'll begin.

Memory saved. Ready to implement whenever you are — just say "go" and I'll start Phase 1 (Issues 1, 3, 4) in parallel.